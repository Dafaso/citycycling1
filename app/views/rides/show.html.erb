<p><%= @ride.ride_name %></p>
<p><%= @ride.date %></p>
<p><%= @ride.milage %></p>

<% if logged_in? && current_user.status == "active" %>
<p>Start Info: <%= @ride.start_info %></p>
<% elsif logged_in? %>
<p>Sign up for this ride to see start info </p>
<% else %>
<p>log in</p>
<% end %>

<p><%= @ride.description %></p>

<%= link_to "New Ride", new_ride_path %>

<%= link_to 'Log Out', logout_path, method: :delete %><br>
<%= link_to 'Log In', '../login' %>
